{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "agent_home": {
      "description": "Private state root for the restricted Console Agent.",
      "type": "string"
    },
    "agent_configuration_store": {
      "description": "Host-owned SQLite authority for durable Console Agent configuration proposals, publication history, and desired-state recovery.",
      "type": "string"
    },
    "allowed_tools": {
      "description": "Reviewed model-visible Tools admitted to the Console Agent.",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "connected_agent_label": {
      "description": "Identity label for the optional App Agent contributed to this Console.",
      "type": "string"
    },
    "console_agent_url": {
      "description": "Clean loopback HTTP origin of the separately released Console Agent process.",
      "type": "string"
    },
    "agent_control_token_file": {
      "description": "Optional Host-private file containing the shared Agent bearer token. The token itself never enters the Resolved App Plan.",
      "type": "string"
    },
    "connected_agent_auth_connections": {
      "description": "Explicitly authorizes Console to manage App Agent authentication connections.",
      "type": "boolean"
    },
    "connected_agent_plugin_configuration": {
      "description": "Explicitly authorizes Console to consume the App Agent Host's Plugin configuration capability.",
      "type": "boolean"
    },
    "connected_agent_plugin_lifecycle": {
      "description": "Explicitly authorizes Console to consume the App Agent Host's trusted Plugin package lifecycle capability.",
      "type": "boolean"
    },
    "connected_agent_url": {
      "description": "Clean loopback HTTP origin used by the optional App Agent Adapter.",
      "type": "string"
    },
    "managed_app_root": {
      "description": "Lenso App whose ordinary Plugin Root is managed by Console.",
      "type": "string"
    },
    "trusted_plugin_bundles": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Host-private trusted Bundle catalog keyed by opaque entry ID. Paths resolve from the launcher directory and are never exposed to Console Agent.",
      "type": "object"
    },
    "web_root": {
      "description": "Directory containing the built Console Shell index.html.",
      "type": "string"
    },
    "managed_apps": {
      "type": "array",
      "description": "Explicit Plugin management connections for non-Agent Apps and the Console extension authority. These targets do not become conversational Agents.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "label", "origin"],
        "properties": {
          "id": {
            "type": "string"
          },
          "label": {
            "type": "string"
          },
          "origin": {
            "type": "string"
          },
          "consoleExtensions": {
            "type": "boolean",
            "default": false
          },
          "controlTokenEnv": {
            "type": "string"
          }
        }
      },
      "default": []
    },
    "local_projects": {
      "description": "Optional local project launcher paths owned by the reference Console Host.",
      "type": "object",
      "additionalProperties": false,
      "required": ["binary", "root", "template"],
      "properties": {
        "binary": {
          "type": "string"
        },
        "root": {
          "type": "string"
        },
        "template": {
          "type": "string"
        }
      }
    },
    "require_user_session": {
      "type": "boolean",
      "default": false,
      "description": "Require the explicitly bound Lenso Auth provider for Console API requests. Local mode has no Auth binding and remains loopback-only."
    },
    "administrator_subjects": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true,
      "default": [],
      "description": "Auth subjects allowed to administer Console and access shared Host Agent interfaces. Workspace Plugins enforce their own business authorization."
    },
    "member_workspace_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "default": [],
      "description": "Workspace IDs exposed to authenticated members. These Plugins must enforce their own business authorization."
    }
  },
  "required": [
    "agent_home",
    "agent_configuration_store",
    "allowed_tools",
    "connected_agent_label",
    "connected_agent_plugin_configuration",
    "connected_agent_plugin_lifecycle",
    "connected_agent_url",
    "console_agent_url",
    "managed_app_root",
    "trusted_plugin_bundles",
    "web_root"
  ],
  "type": "object"
}
