# Projects alignment review — 2026-09-12

## Scope and reference

Correct the workspace switch label against the sidebar section label, and the project overview Issues link's hover surface against its heading. Reference: the user's approved Projects sidebar and reported overview screenshot. Keep the existing Lenso Button/Menu/Sidebar components; the full-width Issues anchor is a page-specific row, not the compact inline QuickLink primitive.

## Results

- Sidebar text geometry: pass. Console browser integration renders the compiled plugin with real Lenso component CSS. DOM Range text starts differ by at most 1 CSS px. Header/content/trigger insets now use named layout variables in one scope.
- Overview text geometry: pass. Heading and row label share an anchor, background extends 8px before the label, and hover/focus do not move text.
- Desktop 1440×900 and narrow 390×900: pass in the standalone fixture. Both light and dark were exercised; no horizontal overflow. Hover and focus screenshots are generated by `web/tests/projects.spec.ts` into Playwright test output. Light narrow and dark desktop/narrow screenshots were inspected.
- Shared header/navigation regression: pass. The same test continues through project issues and issue details after the geometry checks.
- Console integration regression: pass. The stateful Agent context and team navigation test still passes with the alignment assertion.
- Build/type/lint: Projects frontend build and Console TypeScript/focused lint passed. Document relative links and whitespace checked.
- Test runner limitation: Console Vitest reports a teardown timeout after its browser test passes; this is not reported as a clean runner shutdown.

## Limits

The screenshots use isolated browser fixtures, not production data. Assets are imported into the local Console preview, but the latest live Safari view was not re-inspected in this change. No backend behavior was changed or newly certified. Historical handwritten breadcrumbs and other page compositions have not been globally migrated by this focused correction; new work must follow the component discovery rule and track remaining debt at its owner.
