---
"@lenso/console-web": patch
---

Remove the Welcome demonstration from production Console binaries. Activate optional Workspaces only through explicit App defaults or Plugin Root configuration, while retaining standalone Observe and shared contract packages outside the Shell service workspace.
