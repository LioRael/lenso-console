---
"@lenso/console-web": minor
---

Show the selected Agent's working directory with Agent-scoped task creation and history. Add recorded task Changes with earlier snapshots and truncation indicators, and keep task navigation usable on narrow screens. Directory context requires Lenso Agent 0.1.7; older Agents remain compatible. Accept Session instruction revision events when switching coding Profiles.
