---
"@lenso/console-web": patch
---

Keep Console-owned Story and Services surfaces visible for connected Systems without requiring separately reconciled dynamic artifacts. Linked Module surfaces may now bind to an enrolled owner Service, so Auth requests keep targeting the Host even when another Service is selected.
