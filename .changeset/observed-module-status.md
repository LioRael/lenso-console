---
"@lenso/console-web": patch
---

Keep Modules without a runtime observation unmanaged instead of reporting a false connected state, and expose missing Auth or Story Console UI artifacts as actionable Surface availability failures.
