---
"@lenso/console-web": minor
---

Unify terminal commands under lenso-agent: use auth, profiles, sessions, doctor, and run directly. Delegate terminal parsing to the native Agent while preserving the cli compatibility entrypoint and Web launch behavior. Bundle native Agent 0.1.11 for both supported platforms.
