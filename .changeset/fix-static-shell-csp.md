---
"@lenso/console-web": patch
---

Allow the statically served TanStack Start bootstrap script through a
content-hashed Content-Security-Policy so the Console Service can hydrate and
authenticate in a real browser.
