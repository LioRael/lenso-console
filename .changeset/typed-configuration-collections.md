---
"@lenso/console-web": minor
---

Edit typed configuration arrays and dynamic object keys with native field controls. Preserve array ordering and string whitespace, prevent duplicate-key overwrites, and keep sensitive collection values out of input hydration.
