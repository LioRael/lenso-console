---
"@lenso/console-web": minor
---

Add Plugin-configured Console login, per-request Auth checks, secure session handling, and explicit administrator access. Discover password and enterprise SSO methods from Auth bindings without exposing shared Host authority to every authenticated account.
